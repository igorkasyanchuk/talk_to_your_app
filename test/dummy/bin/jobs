#!/usr/bin/env ruby
# frozen_string_literal: true
# Starts the Solid Queue supervisor (workers + dispatcher + scheduler), which
# also runs the recurring tasks in config/recurring.yml.
require_relative "../config/environment"
require "solid_queue/cli"
SolidQueue::Cli.start(ARGV.empty? ? %w[start] : ARGV)
