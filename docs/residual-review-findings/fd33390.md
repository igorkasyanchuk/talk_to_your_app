# Residual code-review findings — accepted for v1

Run: Tier 2 `ce-code-review` (autofix) over the full gem, HEAD `fd33390`.
The P0/P1 findings and the user-selected items (R1 Flipper gates, R2 MySQL
timeout, R3 per-principal authorization) were all fixed. The items below were
reviewed and **accepted as known residuals** for v1 — none are correctness or
security defects.

| # | Severity | File | Finding | Disposition |
|---|----------|------|---------|-------------|
| R4 | P2 | `plugins/flipper/tools/enable_flag.rb`, `disable_flag.rb` | enable/disable then read-back are two operations, not atomic; a caching Flipper adapter could return a stale read. | Accepted. `flipper-active_record` (the documented adapter) persists synchronously and is not cached, so the read-back is accurate. Revisit if a caching adapter is officially supported. |
| R5a | P3 | `railtie.rb:13` | `config.after_initialize` nested inside an `initializer` block is non-idiomatic. | Accepted. Works correctly; the explicit `after: :load_config_initializers` ordering is intentional. |
| R5b | P3 | `connection_registry.rb`, `audit_logger.rb`, `plugin_registry.rb` | `module_function` leaves internal helpers publicly callable (semver surface). | Accepted for v1; tighten with `private_class_method` in a later pass if the surface causes churn. |
| R5c | P3 | `plugins/jobs/tools/recent_jobs.rb`, `failed_jobs.rb` | `MAX_LIMIT = 500` duplicated in two tools. | Accepted; two-line duplication, low churn risk. |
| R5d | P3 | `auth/middleware.rb:52` | Origin allowlist is exact-string match (no trailing-slash / default-port normalization). | Accepted; documented that operators must list the exact browser Origin. |

Residual risks documented in the README security model (no per-key tool scopes
beyond the optional `config.authorize` hook; unrestricted Flipper actor
targeting) are intentional v1 scope and not tracked here.
